// settings.gradle

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        // You can add more repositories here if needed
    }
}

rootProject.name = "RentifyApp"
include ':app'
